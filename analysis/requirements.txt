# numpy<2 is load-bearing: under numpy>=2 the UpSet panel in
# figure_combined_cell_lines_atlas crashes at render (matplotlib text.py
# float(array)); upsetplot 0.9 feeds it an array. All figures render on 1.26.
numpy>=1.24,<2
pandas>=2.0,<3
matplotlib>=3.7,<4
matplotlib-venn>=1.1,<2
upsetplot>=0.9,<1
requests>=2.31,<3
pytest>=8.0
# ProteoBench supplies the quant-accuracy metric pipeline (QuantScoresHYE) and
# the authoritative per-species expected ratios used by the Fig 2 accuracy panel.
proteobench>=0.15,<0.16
pyarrow>=15.0,<22
# fsspec ships with HTTPFileSystem used for streaming the PXD030304 33 GB
# diann_report.parquet over PRIDE FTP without staging it to local disk.
fsspec[http]>=2024.1.0
